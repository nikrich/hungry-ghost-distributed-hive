// Placeholder handler — replaced with real code during CI/CD bundling
exports.handler = async event => {
  return {
    statusCode: 501,
    body: JSON.stringify({ message: 'Placeholder — deploy real handler via CI/CD' }),
  };
};
